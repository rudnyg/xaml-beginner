<Page
    x:Class="RestaurantManager.ExpeditePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:RestaurantManager.Models"
    mc:Ignorable="d">
    <Page.DataContext>
        <model:DataManager/>
    </Page.DataContext>
    <Grid Background="#FF000000">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height=".25*" />     
        </Grid.RowDefinitions>
        <CommandBar>
            <AppBarButton Icon="Home" Label="Home"  x:Name="HomeButton" Click="HomeButton_OnClick"/>
        </CommandBar>
        <GridView Grid.Column="0" Grid.Row="0" Background="Black" 
                  ItemsSource="{Binding OrderItems}"/>
        <Button Grid.Row="1" Content="Clear All Orders"   x:Name="ClearButton" Click="ClearButton_OnClick" VerticalAlignment="Center" HorizontalAlignment="Center"/>

    </Grid>
</Page>
